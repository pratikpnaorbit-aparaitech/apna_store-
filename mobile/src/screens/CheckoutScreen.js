import { Ionicons } from "@expo/vector-icons"; import { useState } from "react"; import { Alert, Pressable, ScrollView, StyleSheet, Text, View } from "react-native"; import Screen from "../components/Screen"; import PrimaryButton from "../components/PrimaryButton"; import { api, messageFromError } from "../api/client"; import { useAddress } from "../context/AddressContext"; import { useAuth } from "../context/AuthContext"; import { useCart } from "../context/CartContext"; import { colors, shadow } from "../theme";
export default function CheckoutScreen({ route, navigation }) { const { user } = useAuth(); const { selectedAddress } = useAddress(); const { items, subtotal, clear } = useCart(); const [loading, setLoading] = useState(false); const couponCode = route.params?.couponCode || ""; const delivery = 40; const taxes = Number((subtotal * .05).toFixed(2)); const discount = couponCode === "TRY50" && subtotal >= 99 ? Number(Math.min(subtotal * .5, 150).toFixed(2)) : 0; const total = subtotal + delivery + taxes - discount; const place = async () => { if (!selectedAddress) return navigation.navigate("Location"); setLoading(true); try { const payload = { items: items.map((item) => ({ productId: item._id, name: item.name, quantity: item.quantity })), storeId: items[0]?.storeId?._id || items[0]?.storeId, couponCode, address: { name: user?.name || "Customer", phone: user?.mobile || "", street: selectedAddress.address, city: selectedAddress.city || "", state: selectedAddress.state || "", pincode: selectedAddress.pincode || "" }, customerLocation: selectedAddress.latitude ? { latitude: selectedAddress.latitude, longitude: selectedAddress.longitude } : undefined }; const { data } = await api.post("/orders/place", payload); clear(); navigation.replace("OrderSuccess", { order: data.order }); } catch (e) { Alert.alert("Couldn’t place order", messageFromError(e)); } finally { setLoading(false); } }; return <Screen><View style={styles.header}><Pressable onPress={() => navigation.goBack()}><Ionicons name="arrow-back" size={22} color={colors.ink} /></Pressable><Text style={styles.title}>Checkout</Text></View><ScrollView contentContainerStyle={styles.content}><Text style={styles.sectionTitle}>Delivery address</Text><Pressable onPress={() => navigation.navigate("Location")} style={styles.card}><View style={styles.icon}><Ionicons name="location" size={21} color={colors.purple} /></View><View style={{ flex: 1 }}><Text style={styles.cardTitle}>{selectedAddress?.label || "Choose address"}</Text><Text style={styles.cardSub}>{selectedAddress ? `${selectedAddress.address}, ${selectedAddress.city || ""}` : "Add a delivery address"}</Text></View><Ionicons name="chevron-forward" size={18} color={colors.muted} /></Pressable><Text style={styles.sectionTitle}>Contact</Text><View style={styles.card}><View style={styles.icon}><Ionicons name="person" size={21} color={colors.purple} /></View><View><Text style={styles.cardTitle}>{user?.name}</Text><Text style={styles.cardSub}>{user?.email}</Text></View></View><Text style={styles.sectionTitle}>Payment method</Text><View style={[styles.card, { borderColor: colors.purple }]}><View style={styles.icon}><Ionicons name="cash-outline" size={21} color={colors.purple} /></View><View style={{ flex: 1 }}><Text style={styles.cardTitle}>Cash on delivery</Text><Text style={styles.cardSub}>Pay when your order arrives</Text></View><Ionicons name="checkmark-circle" size={23} color={colors.success} /></View><Text style={styles.sectionTitle}>Payment summary</Text><View style={styles.summary}>{[["Item total", subtotal], ["Delivery", delivery], ["Taxes & handling", taxes], ...(discount ? [["Coupon discount", -discount]] : [])].map(([label, value]) => <View key={label} style={styles.row}><Text style={styles.label}>{label}</Text><Text style={[styles.value, value < 0 && { color: colors.success }]}>{value < 0 ? `-₹${Math.abs(value).toFixed(2)}` : `₹${Number(value).toFixed(2)}`}</Text></View>)}<View style={styles.divider} /><View style={styles.row}><Text style={styles.totalLabel}>Total</Text><Text style={styles.totalValue}>₹{total.toFixed(2)}</Text></View></View></ScrollView><View style={styles.footer}><View><Text style={styles.pay}>₹{total.toFixed(2)}</Text><Text style={styles.paySub}>TOTAL TO PAY</Text></View><PrimaryButton title="Place order" loading={loading} onPress={place} style={{ width: 190 }} /></View></Screen>; }
const styles = StyleSheet.create({ header: { height: 62, paddingHorizontal: 18, backgroundColor: "white", flexDirection: "row", alignItems: "center", gap: 16, borderBottomWidth: 1, borderBottomColor: colors.border }, title: { color: colors.ink, fontSize: 20, fontWeight: "900" }, content: { padding: 15, paddingBottom: 30 }, sectionTitle: { color: colors.ink, fontSize: 15, fontWeight: "900", marginTop: 17, marginBottom: 10 }, card: { backgroundColor: "white", borderRadius: 17, borderWidth: 1, borderColor: colors.border, padding: 14, flexDirection: "row", alignItems: "center", gap: 12 }, icon: { width: 42, height: 42, borderRadius: 14, backgroundColor: colors.purpleSoft, alignItems: "center", justifyContent: "center" }, cardTitle: { color: colors.ink, fontWeight: "900", fontSize: 13 }, cardSub: { color: colors.muted, fontSize: 11, lineHeight: 16, marginTop: 3 }, summary: { backgroundColor: "white", borderRadius: 18, padding: 16, ...shadow }, row: { flexDirection: "row", justifyContent: "space-between", marginVertical: 7 }, label: { color: colors.muted, fontSize: 12 }, value: { color: colors.ink, fontWeight: "700", fontSize: 12 }, divider: { height: 1, backgroundColor: colors.border, marginVertical: 7 }, totalLabel: { color: colors.ink, fontWeight: "900", fontSize: 15 }, totalValue: { color: colors.ink, fontWeight: "900", fontSize: 16 }, footer: { backgroundColor: "white", borderTopWidth: 1, borderTopColor: colors.border, padding: 13, paddingHorizontal: 18, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, pay: { fontSize: 17, fontWeight: "900", color: colors.ink }, paySub: { color: colors.muted, fontSize: 9, marginTop: 2 } });
