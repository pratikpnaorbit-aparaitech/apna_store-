import { Ionicons } from "@expo/vector-icons"; import { useCallback, useEffect, useState } from "react"; import { Pressable, ScrollView, StyleSheet, Text, View } from "react-native"; import Screen from "../components/Screen"; import StateView from "../components/StateView"; import { api, messageFromError } from "../api/client"; import { useAuth } from "../context/AuthContext"; import { colors, shadow } from "../theme";
const statusColors = { Delivered: ["#DCFCE7", "#15803D"], Cancelled: ["#FEE2E2", "#B91C1C"], "Out for Delivery": ["#EDE9FE", "#6D28D9"], Preparing: ["#FEF3C7", "#B45309"], Confirmed: ["#DBEAFE", "#1D4ED8"], Placed: ["#F3E8FF", "#7E22CE"] };
export default function OrdersScreen({ navigation }) { const { user } = useAuth(); const [orders, setOrders] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const load = useCallback(async () => { try { const { data } = await api.get(`/orders/user/${user.id}`); setOrders(data); setError(""); } catch (e) { setError(messageFromError(e)); } finally { setLoading(false); } }, [user.id]); useEffect(() => { load(); }, [load]); return <Screen><View style={styles.header}><Pressable onPress={() => navigation.goBack()}><Ionicons name="arrow-back" size={22} color={colors.ink} /></Pressable><Text style={styles.title}>My orders</Text></View>{loading ? <StateView loading /> : error ? <StateView icon="cloud-offline-outline" title="Orders unavailable" message={error} action="Try again" onAction={load} /> : !orders.length ? <StateView icon="receipt-outline" title="No orders yet" message="Your orders will appear here after checkout." action="Start shopping" onAction={() => navigation.navigate("Main", { screen: "HomeTab" })} /> : <ScrollView contentContainerStyle={styles.content}>{orders.map((order) => { const palette = statusColors[order.status] || statusColors.Placed; return <View key={order._id} style={styles.card}><View style={styles.top}><View><Text style={styles.orderId}>ORDER #{String(order._id).slice(-6).toUpperCase()}</Text><Text style={styles.date}>{new Date(order.createdAt).toLocaleDateString()}</Text></View><Text style={[styles.status, { backgroundColor: palette[0], color: palette[1] }]}>{order.status}</Text></View><View style={styles.items}>{order.items?.slice(0, 3).map((item) => <Text key={item.productId} numberOfLines={1} style={styles.item}>• {item.name} × {item.quantity}</Text>)}</View><View style={styles.bottom}><Text style={styles.total}>₹{Number(order.totalAmount).toFixed(2)}</Text><Text style={styles.payment}>{order.paymentMethod} • {order.paymentStatus}</Text></View></View>; })}</ScrollView>}</Screen>; }
const styles = StyleSheet.create({ header: { height: 62, backgroundColor: "white", paddingHorizontal: 18, flexDirection: "row", alignItems: "center", gap: 15, borderBottomWidth: 1, borderBottomColor: colors.border }, title: { color: colors.ink, fontSize: 20, fontWeight: "900" }, content: { padding: 14, paddingBottom: 30 }, card: { backgroundColor: "white", borderRadius: 18, padding: 15, marginBottom: 12, borderWidth: 1, borderColor: colors.border, ...shadow }, top: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, orderId: { color: colors.ink, fontWeight: "900", fontSize: 12 }, date: { color: colors.muted, fontSize: 10, marginTop: 4 }, status: { borderRadius: 20, paddingHorizontal: 9, paddingVertical: 6, fontSize: 9, fontWeight: "900" }, items: { marginVertical: 13, paddingVertical: 10, borderTopWidth: 1, borderBottomWidth: 1, borderColor: colors.border }, item: { color: colors.muted, fontSize: 11, lineHeight: 19 }, bottom: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, total: { color: colors.ink, fontSize: 16, fontWeight: "900" }, payment: { color: colors.muted, fontSize: 10 } });
