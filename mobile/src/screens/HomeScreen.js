import { Ionicons } from "@expo/vector-icons";
import { LinearGradient } from "expo-linear-gradient";
import { useCallback, useEffect, useState } from "react";
import { FlatList, Pressable, RefreshControl, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import Screen from "../components/Screen"; import ProductCard from "../components/ProductCard"; import StateView from "../components/StateView"; import { api, messageFromError } from "../api/client"; import { useAddress } from "../context/AddressContext"; import { colors, getCategoryVisual, shadow } from "../theme";

export default function HomeScreen({ navigation }) {
  const { selectedAddress } = useAddress(); const [products, setProducts] = useState([]); const [stores, setStores] = useState([]); const [loading, setLoading] = useState(true); const [refreshing, setRefreshing] = useState(false); const [error, setError] = useState(""); const [search, setSearch] = useState("");
  const load = useCallback(async (refresh = false) => { refresh ? setRefreshing(true) : setLoading(true); setError(""); try { const [p, s] = await Promise.all([api.get("/inventory/public"), api.get("/stores/public")]); setProducts(Array.isArray(p.data) ? p.data : []); setStores(s.data?.data || []); } catch (e) { setError(messageFromError(e, "Could not load products.")); } finally { setLoading(false); setRefreshing(false); } }, []);
  useEffect(() => { load(); }, [load]);
  const categories = [...new Set(products.map((item) => item.category).filter(Boolean))].slice(0, 8);
  const submitSearch = () => search.trim() && navigation.navigate("Products", { search: search.trim(), title: `Results for “${search.trim()}”` });
  if (loading) return <Screen><StateView loading /></Screen>;
  if (error && !products.length) return <Screen><StateView icon="cloud-offline-outline" title="Couldn’t load the store" message={error} action="Try again" onAction={load} /></Screen>;
  return <Screen><ScrollView showsVerticalScrollIndicator={false} refreshControl={<RefreshControl refreshing={refreshing} onRefresh={() => load(true)} tintColor={colors.purple} />}><LinearGradient colors={["#F2E8FF", "#FFF7FC"]} style={styles.hero}><View style={styles.top}><Pressable onPress={() => navigation.navigate("Location")} style={{ flex: 1 }}><Text style={styles.time}>Delivery in 6 minutes ⚡</Text><View style={styles.addressRow}><Ionicons name="location" size={14} color={colors.purple} /><Text numberOfLines={1} style={styles.address}>{selectedAddress?.address || "Choose delivery address"}</Text><Ionicons name="chevron-down" size={14} color={colors.muted} /></View></Pressable><Pressable onPress={() => navigation.navigate("ProfileTab")} style={styles.avatar}><Ionicons name="person" size={20} color={colors.purple} /></Pressable></View><View style={styles.search}><Ionicons name="search" size={20} color={colors.muted} /><TextInput value={search} onChangeText={setSearch} onSubmitEditing={submitSearch} returnKeyType="search" placeholder="Search for products" placeholderTextColor="#938C9B" style={styles.searchInput} /><Ionicons name="mic-outline" size={20} color={colors.purple} /></View></LinearGradient><View style={styles.body}><ScrollView horizontal pagingEnabled showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: 12 }}>{[["Big savings, tiny wait","Up to 50% OFF","#6D28D9","🎁"],["Fresh picks daily","Farm-fresh essentials","#E91E8C","🥬"],["Free delivery","On your first order","#0F9D75","🛵"]].map((banner) => <LinearGradient key={banner[0]} colors={[banner[2], banner[2] + "CC"]} style={styles.banner}><View><Text style={styles.bannerTitle}>{banner[0]}</Text><Text style={styles.bannerSub}>{banner[1]}</Text><Pressable onPress={() => navigation.navigate("OffersTab")} style={styles.shop}><Text style={styles.shopText}>SHOP NOW</Text></Pressable></View><Text style={styles.bannerEmoji}>{banner[3]}</Text></LinearGradient>)}</ScrollView><SectionHeader title="Shop by category" action="See all" onPress={() => navigation.navigate("CategoriesTab")} /><View style={styles.categories}>{categories.map((category) => { const visual = getCategoryVisual(category); return <Pressable key={category} onPress={() => navigation.navigate("Products", { category, title: category })} style={styles.category}><View style={[styles.categoryIcon, { backgroundColor: visual[1] }]}><Ionicons name={visual[0]} size={27} color={colors.purple} /></View><Text numberOfLines={2} style={styles.categoryText}>{category}</Text></Pressable>; })}</View>{stores.length ? <><SectionHeader title="Stores near you" action="View all" onPress={() => navigation.navigate("Stores")} /><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: 11 }}>{stores.slice(0, 6).map((store) => <Pressable key={store._id} onPress={() => navigation.navigate("Products", { storeId: store._id, title: store.name })} style={styles.store}><View style={styles.storeLogo}><Text style={{ fontSize: 25 }}>🏪</Text></View><View style={{ flex: 1 }}><Text numberOfLines={1} style={styles.storeName}>{store.name}</Text><Text style={styles.storeMeta}>10–15 min • 4.5 ★</Text></View></Pressable>)}</ScrollView></> : null}<SectionHeader title="Popular near you" action="See all" onPress={() => navigation.navigate("Products", { title: "Popular products" })} /><FlatList scrollEnabled={false} data={products.slice(0, 8)} numColumns={2} columnWrapperStyle={{ justifyContent: "space-between", marginBottom: 12 }} keyExtractor={(item) => item._id} renderItem={({ item }) => <ProductCard product={item} compact onPress={() => navigation.navigate("ProductDetail", { product: item })} />} /></View></ScrollView></Screen>;
}
function SectionHeader({ title, action, onPress }) { return <View style={styles.section}><Text style={styles.sectionTitle}>{title}</Text><Pressable onPress={onPress}><Text style={styles.sectionAction}>{action} ›</Text></Pressable></View>; }
const styles = StyleSheet.create({ hero: { padding: 18, paddingBottom: 20, borderBottomLeftRadius: 26, borderBottomRightRadius: 26 }, top: { flexDirection: "row", alignItems: "center", marginBottom: 18 }, time: { color: colors.ink, fontSize: 18, fontWeight: "900" }, addressRow: { flexDirection: "row", alignItems: "center", gap: 4, marginTop: 5, maxWidth: "95%" }, address: { color: colors.muted, fontSize: 12, maxWidth: "78%" }, avatar: { width: 43, height: 43, borderRadius: 15, backgroundColor: "white", alignItems: "center", justifyContent: "center", ...shadow }, search: { height: 52, borderRadius: 17, backgroundColor: "white", flexDirection: "row", alignItems: "center", paddingHorizontal: 15, ...shadow }, searchInput: { flex: 1, marginHorizontal: 9, fontSize: 14, color: colors.ink }, body: { padding: 16, paddingBottom: 30 }, banner: { width: 315, height: 155, borderRadius: 23, padding: 20, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, bannerTitle: { color: "white", fontSize: 21, fontWeight: "900", maxWidth: 190 }, bannerSub: { color: "rgba(255,255,255,.82)", marginTop: 5, fontSize: 12 }, bannerEmoji: { fontSize: 58 }, shop: { backgroundColor: "white", paddingHorizontal: 12, paddingVertical: 8, borderRadius: 10, alignSelf: "flex-start", marginTop: 14 }, shopText: { color: colors.purple, fontSize: 10, fontWeight: "900" }, section: { marginTop: 25, marginBottom: 13, flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, sectionTitle: { color: colors.ink, fontSize: 18, fontWeight: "900" }, sectionAction: { color: colors.purple, fontSize: 12, fontWeight: "800" }, categories: { flexDirection: "row", flexWrap: "wrap", justifyContent: "space-between", rowGap: 14 }, category: { width: "23%", alignItems: "center" }, categoryIcon: { width: 68, height: 65, borderRadius: 20, alignItems: "center", justifyContent: "center" }, categoryText: { color: colors.ink, textAlign: "center", fontSize: 10.5, fontWeight: "700", marginTop: 7, lineHeight: 14 }, store: { width: 210, backgroundColor: "white", borderWidth: 1, borderColor: colors.border, borderRadius: 17, padding: 12, flexDirection: "row", alignItems: "center", gap: 11 }, storeLogo: { width: 48, height: 48, borderRadius: 15, backgroundColor: colors.purpleSoft, alignItems: "center", justifyContent: "center" }, storeName: { color: colors.ink, fontWeight: "900" }, storeMeta: { color: colors.muted, fontSize: 11, marginTop: 4 } });
